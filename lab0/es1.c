#include <stdio.h>


int main() {
    char text[50]="ciao";

    //scanf("%s", &text);

    //fscanf(stdin, "%s", text);

    //fgets(text, 10, stdin);

    //gets(text);

/*    text[0]=getchar();
    text[2]=getc(stdin);
    text[1]=fgetc(stdin);
    text[3]='\0';*/




    //printf("%s", text);

    //fprintf(stdout, "%s", text);

    //putchar(text[0]);

    //putc(text[0], stdout);

    //fputc(text[0], stdout);

    //puts(text);

    //fputs(text, stdout);

    return 0;
}
